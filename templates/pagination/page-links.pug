- var classes = '<perch:if exists="selected">active</perch:if>'

perch:before
  | <ul>
  
li(class!=classes)
  perch:if(exists="spacer")
    perch:pages(id="page_number")/
    perch:else/
    a(href!='<perch:pages id="url" />')
      perch:pages(id="page_number")/
      
perch:after
  | </ul>